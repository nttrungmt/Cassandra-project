
for(int i =1; i<= 10 ; i++) {
	SELECT o_id,c_id,ol_id,ol_amount FROM orders WHERE w_id = 1 and d_id =1 and o_carrier_id = 0 

	N = min(o_id);
	c_id,ol_id,ol_amount -->corresponding to that o_id
	//ORDER BY o_id DESC LIMIT 1;

	UPDATE orders SET o_carrier_id = carrier_id, ol_delivery_d = dateOf(now()) WHERE w_id =1 and d_id = i and o_id =1 and c_id = 1 and ol_id = 1;

	UPDATE customer SET c_balance = c_balance + B, c_delivery_cnt = c_delivery_cnt+1 WHERE w_id = 1 and d_id = 1 and c_id = 1;
}
